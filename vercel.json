{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [{ "src": "/(.*)", "dest": "index.js" }],
  "env": {
    "NODE_ENV": "production"
  },
  "regions": ["bom1"]
}
